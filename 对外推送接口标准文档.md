# 对外推送接口标准文档

> 版本：v1.0  
> 最后更新：2026-06-15  
> 推送目标：`ylcs.htface.cn:33895`  
> 推送脚本：`scripts/push/push_to_external.py`

---

## 目录

1. [概述](#1-概述)
2. [接口规范](#2-接口规范)
   - 2.1 更新学生信息 (updateStudent)
   - 2.2 批量添加表情数据 (AddEmotion)
3. [数据来源与映射](#3-数据来源与映射)
4. [图片处理规则](#4-图片处理规则)
5. [情绪映射表](#5-情绪映射表)
6. [推送流程](#6-推送流程)
7. [错误处理](#7-错误处理)
8. [附录：审核表结构](#8-附录审核表结构)

---

## 1. 概述

本平台（明眸学生情绪感知平台）将课堂人脸检测与情绪分析结果推送到外部平台。推送包含两类数据：

| 数据类型 | 推送方法 | 说明 |
|---------|---------|------|
| 学生信息 | `updateStudent` | 学生编号、姓名、人脸照片（base64） |
| 情绪记录 | `AddEmotion` | 抓拍时间、情绪分析结果、人脸小图、场景大图（base64） |

推送使用通用 API 执行器模式：`POST /api/Page/Execute`，通过 `pageID` + `method` 区分不同的操作。

---

## 2. 接口规范

### 2.1 公共请求参数

所有请求共享以下顶层参数：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `pageID` | string | 是 | 固定为 `Emotion` |
| `method` | string | 是 | `updateStudent` 或 `AddEmotion` |

### 2.2 基础 URL

```
POST http://ylcs.htface.cn:33895/api/Page/Execute
```

### 2.3 更新学生信息 (updateStudent)

**功能描述**：上传或更新学生信息，包含学生的人脸照片。

**请求参数**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `pageID` | string | 是 | `Emotion` |
| `method` | string | 是 | `updateStudent` |
| `student_code` | string | 是 | 学生编号，与 AddEmotion 中的 `student_code` 对应 |
| `student_name` | string | 是 | 学生姓名 |
| `ImageUrl` | array[string] | 是 | 学生人脸照片列表（base64 编码，最多 5 张） |

**请求示例**：

```json
{
  "pageID": "Emotion",
  "method": "updateStudent",
  "student_code": "auto_1_001",
  "student_name": "student001",
  "ImageUrl": [
    "/9j/4AAQSkZJRgABAQAAAQABAAD...",
    "/9j/4AAQSkZJRgABAQAAAQABAAD..."
  ]
}
```

**成功响应**：

```json
{
  "success": true,
  "message": "SUCESS",
  "status": 200,
  "response": {
    "student_code": "auto_1_001",
    "updated": true
  }
}
```

**失败响应**：

```json
{
  "success": false,
  "error": "错误描述信息"
}
```

### 2.4 批量添加表情数据 (AddEmotion)

**功能描述**：一次请求中上传多条情绪识别记录。每条记录包含抓拍小图、大图、情绪分析结果、视线方向等。支持批量提交，建议单次不超过 200 条。

**请求参数**：

整个请求体中，需要在根对象中包含一个名为 `emotions` 的数组，数组中的每个元素代表一条表情数据记录。

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `pageID` | string | 是 | `Emotion` |
| `method` | string | 是 | `AddEmotion` |
| `emotions` | array | 是 | 情绪记录数组，每项包含以下字段 |

**emotions 数组中每条记录的字段定义**：

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `Id` | number | 是 | 记录 ID，唯一标识本条情绪记录 |
| `CameraCode` | string | 是 | 摄像机编码（摄像头唯一标识） |
| `student_code` | string | 是 | 学生 ID，与 updateStudent 中的 student_code 对应 |
| `SmallPic` | string | 是 | 抓拍的学生小图（base64 编码的裁剪人脸图片） |
| `CaptureTime` | string | 是 | 抓拍时间，格式 `YYYY-MM-DD HH:MM:SS` |
| `ImageUrl` | string | 是 | 大图（base64 编码的全景图片，640px 缩放） |
| `Confidence` | string | 是 | 置信度，如 `"0.92"` |
| `score` | number | 否 | 得分，没有则传 0 |
| `color` | string | 否 | 情绪对应的颜色标识，没有则传 `""` |
| `Emotion` | string | 是 | 表情代码 |
| `GazeDirection` | string | 否 | 视线方向，没有则传 `""` |
| `created_at` | string | 是 | 分析时间，格式 `YYYY-MM-DD HH:MM:SS` |

**请求示例**：

```json
{
  "pageID": "Emotion",
  "method": "AddEmotion",
  "emotions": [
    {
      "Id": 221,
      "CameraCode": "CAM_DEFAULT",
      "student_code": "auto_1_001",
      "SmallPic": "/9j/4AAQSkZJRgABAQAAAQABAAD...",
      "CaptureTime": "2026-06-15 07:30:25",
      "ImageUrl": "/9j/4AAQSkZJRgABAQAAAQABAAD...",
      "Confidence": "0.76",
      "score": 76,
      "color": "blue",
      "Emotion": "sad",
      "GazeDirection": "",
      "created_at": "2026-06-15 07:30:25"
    }
  ]
}
```

**成功响应（全部插入成功）**：

```json
{
  "success": true,
  "message": "SUCESS",
  "status": 200,
  "response": {
    "success": true,
    "data": {
      "inserted_count": 10,
      "inserted_ids": [221, 248, 402, 473, 492, 502, 529, 678, 750, 838],
      "failed_ids": [],
      "error_details": null
    }
  }
}
```

**部分成功响应**：

```json
{
  "success": true,
  "data": {
    "inserted_count": 1,
    "inserted_ids": [10001],
    "failed_ids": [10002],
    "error_details": {
      "10002": "记录ID已存在"
    }
  }
}
```

**全部失败响应**：

```json
{
  "success": false,
  "error": "批量插入失败：所有记录均无效"
}
```

**响应字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `inserted_count` | number | 成功插入的记录条数 |
| `inserted_ids` | array[number] | 成功插入的记录 ID 列表 |
| `failed_ids` | array[number] | 插入失败的记录 ID 列表 |
| `error_details` | object | 可选，失败详情，键为失败 ID，值为错误原因 |

---

## 3. 数据来源与映射

### 3.1 学生信息 (updateStudent)

| API 字段 | 数据库来源 | SQL/代码 | 说明 |
|---------|-----------|---------|------|
| `student_code` | `student.student_no` | `SELECT s.student_no FROM student s` | 唯一标识 |
| `student_name` | `student.name` | `s.name` | 学生姓名 |
| `ImageUrl` | `face_record.cropped_image_url` | 读取文件 → base64 编码 | 最新 5 张裁剪人脸图 |

**关联查询**：

```sql
SELECT s.student_no, s.name
FROM student s
JOIN face_record fr ON fr.student_id = s.id
GROUP BY s.id
ORDER BY COUNT(fr.id) DESC
```

### 3.2 情绪记录 (AddEmotion)

| API 字段 | 数据库来源 | 说明 |
|---------|-----------|------|
| `Id` | `emotion_record.id` | 情绪记录 ID |
| `CameraCode` | 配置常量 `CAM_DEFAULT` | 摄像头编码 |
| `student_code` | `student.student_no` | 学生编号 |
| `SmallPic` | `face_record.cropped_image_url` | 读取文件 → base64 编码（不缩放） |
| `CaptureTime` | `class_image.capture_time` | 抓拍时间 |
| `ImageUrl` | `class_image.image_url` | 读取原图 → 640px 缩放 → base64 编码 |
| `Confidence` | `emotion_record.dominant_confidence` | 置信度，格式化为 `"0.76"` |
| `score` | `round(dominant_confidence * 100)` | 得分 |
| `color` | 情绪映射 | 颜色标识 |
| `Emotion` | `emotion_record.dominant_emotion` | 经过中文→英文→外部编码映射 |
| `GazeDirection` | 固定空串 `""` | 视线方向（暂未采集） |
| `created_at` | `emotion_record.created_at` | 分析时间 |

**关联查询**：

```sql
SELECT er.id, er.dominant_emotion, er.dominant_confidence, er.created_at,
       fr.cropped_image_url, fr.student_id, s.student_no,
       ci.capture_time, ci.image_url
FROM emotion_record er
JOIN face_record fr ON fr.id = er.face_record_id
LEFT JOIN student s ON s.id = fr.student_id
JOIN class_image ci ON ci.id = fr.class_image_id
WHERE s.student_no IS NOT NULL
ORDER BY er.id ASC
```

### 3.3 过滤条件

推送只包含 `student_id IS NOT NULL` 的 face_record（已关联到学生的记录）。未关联的人脸记录（`face_group`）不推送。

---

## 4. 图片处理规则

### 4.1 图片格式

| 图片类型 | 输出格式 | 缩放规则 | 最大大小 | 典型大小 |
|---------|---------|---------|---------|---------|
| SmallPic（裁剪人脸） | JPEG base64 | 不缩放 | 5MB | ~7KB |
| ImageUrl（原图） | JPEG base64 | 最长边缩放到 640px | 5MB | ~130KB |

### 4.2 路径解析

数据库存储的路径有以下两种格式，脚本自动识别并映射到文件系统：

| 数据库路径示例 | 映射方式 |
|--------------|---------|
| `/images/cropped/官渡一中/初一班/2026-06-15/第7节/face_12345.jpg` | `{PROJECT_ROOT}/emotion-platform/images/cropped/官渡一中/...` |
| `/media/zebra/data/官渡一中初一班-0526/data/官渡一中/初一班/...` | 直接作为绝对路径读取 |

### 4.3 Base64 验证

发送前对 base64 数据进行魔数验证，只解码前 12 字节检查：

| 格式 | 魔数 |
|------|------|
| JPEG | `FF D8 FF` |
| PNG | `89 50 4E 47` |
| WebP | `RIFF .... WEBP` |

### 4.4 异常处理

- 图片文件不存在 → 跳过该条记录（记录 ID 记入 `failed_ids`）
- 图片超出大小限制（5MB）→ 跳过
- 图片缩放失败 → 不缩放，直接 base64 编码原图

### 4.5 时间戳回退

当数据库中的 `capture_time` 或 `created_at` 为 NULL 时，从图片文件路径结构推导：

- 从目录结构提取日期：`/data/官渡一中/初一班/{date}/{period}/...`
- 支持两种日期目录格式：`YYYY-MM-DD`（如 `2026-05-27`）和 `YYYY-MMDD`（如 `2026-0521`）
- 时间部分使用文件修改时间（mtime）

---

## 5. 情绪映射表

### 5.1 中文 → 英文（第一步）

| 数据库存储值 | 中间英文 |
|------------|---------|
| 中性 | neutral |
| 开心 | happy |
| 伤心 | sad |
| 生气 | angry |
| 惊讶 | surprise |
| 恐惧 | fear |
| 厌恶 | disgust |
| 蔑视 | contempt |

### 5.2 英文 → 外部编码（第二步）

| 中间英文 | 推送编码 | 颜色 |
|---------|---------|------|
| happy | happy | green |
| sad | sad | blue |
| angry | angry | red |
| disgust | angry | red |
| surprise | surprised | yellow |
| fear | fearful | purple |
| neutral | calm | cyan |
| contempt | calm | cyan |

### 5.3 缺失处理

- 不支持的输入 → 默认 `calm`
- 无颜色映射 → 空字符串 `""`

---

## 6. 推送流程

### 6.1 推送策略

```
┌─────────────────────────────────────────────┐
│  1. 连接 PostgreSQL                          │
│  2. 加载断点续传 checkpoint                   │
│  3. 推送学生 (push_students)                  │
│     ├─ 查询已关联 student 的学生列表           │
│     ├─ 取每个学生最新 5 张裁剪图 → base64      │
│     └─ POST updateStudent                    │
│  4. 保存 checkpoint                          │
│  5. 推送情绪 (push_emotions)                  │
│     ├─ 查询 student_id 非空的 emotion_record  │
│     ├─ 每条记录: SmallPic + ImageUrl → base64 │
│     ├─ 凑满 batch_size=10 条 → 一批 POST     │
│     └─ 按响应中的 inserted_ids / failed_ids   │
│        更新 checkpoint                        │
│  6. 保存 checkpoint                          │
│  7. 打印汇总                                  │
└─────────────────────────────────────────────┘
```

### 6.2 命令行接口

```bash
# 全量推送
python3 push_to_external.py

# 仅推学生
python3 push_to_external.py --students-only

# 仅推情绪
python3 push_to_external.py --emotions-only

# 干跑（不实际发送）
python3 push_to_external.py --dry-run

# 审核模式（写入本地审核表）
python3 push_to_external.py --audit

# 断点续传
python3 push_to_external.py --resume

# 只推送人脸数最多的 N 个学生
python3 push_to_external.py --top 5

# 从指定 emotion_record ID 开始
python3 push_to_external.py --start-id 50000

# 自定义批次大小
python3 push_to_external.py --batch 20
```

### 6.3 环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DB_HOST` | `localhost` | 数据库地址 |
| `DB_PORT` | `5432` | 数据库端口 |
| `DB_NAME` | `emotion_platform` | 数据库名 |
| `DB_USER` | `emotion` | 数据库用户 |
| `DB_PASSWORD` | `emotion` | 数据库密码 |
| `EXTERNAL_API_BASE` | `http://ylcs.htface.cn:33895/api/Page/Execute` | 外部 API 地址 |
| `PAGE_ID` | `Emotion` | 页面 ID |
| `CAMERA_CODE` | `CAM_DEFAULT` | 摄像头编码 |
| `PROJECT_ROOT` | `/media/zebra/data/官渡一中初一班-0526` | 项目根目录 |

### 6.4 推送数据量（当前环境）

| 指标 | 数值 |
|------|------|
| 学生数 | 20 |
| 情绪记录数 | 1,491 |
| 批次大小 | 10 |
| 单批平均大小 | ~1.5MB |
| 总上传量 | ~200MB |
| 预计耗时 | ~5 分钟 |

---

## 7. 错误处理

### 7.1 重试机制

API 调用失败时自动重试：

| 重试次数 | 间隔 | 超时 |
|---------|------|------|
| 第 1 次 | 3 秒 | 120 秒 |
| 第 2 次 | 3 秒 | 120 秒 |
| 第 3 次 | — | — 放弃，返回失败 |

### 7.2 断点续传

- 推送进度保存在 `/tmp/external_push_checkpoint.json`
- 包含：`pushed_emotion_ids`、`pushed_student_ids`、`failed_emotion_ids`
- 使用 `--resume` 跳过已推送的记录
- 使用原子写入（写入 `.tmp` 后 `os.replace`），崩溃不会损坏 checkpoint

### 7.3 部分失败处理

AddEmotion 接口返回 `inserted_count` + `failed_ids`：

- 成功记录 ID 加入 `pushed_emotion_ids`（下次跳过）
- 失败记录 ID 加入 `failed_emotion_ids`（记录在案，不会自动重推）

### 7.4 字段验证失败处理

- `SmallPic` 文件缺失或格式无效 → 跳过该条情绪记录
- `ImageUrl` 文件缺失或格式无效 → 跳过该条情绪记录
- `CaptureTime` / `created_at` 为 NULL → 从图片文件路径回退推导（见 4.5）

---

## 8. 附录：审核表结构

推送前可使用 `--audit` 模式将数据写入本地审核表，无需调用外部 API 即可验证。

### 8.1 push_audit_students（学生审核表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | SERIAL PK | 自增主键 |
| `student_id` | BIGINT | 学生 ID |
| `student_no` | VARCHAR(20) | 学生编号 |
| `student_name` | VARCHAR(50) | 学生姓名 |
| `image_count` | INTEGER | 图片数（1~5） |
| `image_previews` | TEXT | base64 预览（头 60 + 尾 30 + 长度） |
| `image_paths` | TEXT | 来源文件路径（JSON 数组） |
| `payload_size_bytes` | INTEGER | 请求体大小 |
| `created_at` | TIMESTAMPTZ | 审核时间 |

### 8.2 push_audit_emotions（情绪审核表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | SERIAL PK | 自增主键 |
| `emotion_record_id` | BIGINT | 情绪记录 ID |
| `student_no` | VARCHAR(20) | 学生编号 |
| `capture_time` | VARCHAR(19) | 抓拍时间 |
| `created_at_value` | VARCHAR(19) | 分析时间 |
| `emotion` | VARCHAR(20) | 情绪编码 |
| `confidence` | NUMERIC(5,2) | 置信度 |
| `score` | INTEGER | 得分 |
| `color` | VARCHAR(10) | 颜色 |
| `small_pic_preview` | TEXT | SmallPic 预览 |
| `small_pic_bytes` | INTEGER | SmallPic 解码后字节数 |
| `image_url_preview` | TEXT | ImageUrl 预览 |
| `image_url_bytes` | INTEGER | ImageUrl 解码后字节数 |
| `gaze_direction` | VARCHAR(10) | 视线方向 |
| `payload_size_bytes` | INTEGER | 所属批次请求体大小 |
| `batch_id` | INTEGER | 所属批次编号 |
| `created_at` | TIMESTAMPTZ | 审核时间 |
