# 推送审核报告与命令

> 生成时间：2026-06-15  
> 推送目标：`ylcs.htface.cn:33895`  
> 推送脚本：`scripts/push/push_to_external.py`

---

## 一、审核结论

审核表数据与推送接口完全一致，字段完整，格式正确，无数据丢失。

---

## 二、审核表验证详情

### 2.1 学生表 `push_audit_students`

| student_no | student_name | image_count | payload_size_bytes |
|-----------|--------------|-------------|--------------------|
| auto_1_001 | student001 | 5 | 39,738 |
| auto_1_002 | student002 | 5 | 38,038 |
| auto_1_003 | student003 | 5 | 47,282 |
| auto_1_004 | student004 | 5 | 30,470 |
| auto_1_005 | student005 | 5 | 72,838 |
| auto_1_006 | student006 | 5 | 41,490 |
| auto_1_007 | student007 | 5 | 71,598 |
| auto_1_008 | student008 | 5 | 40,906 |
| auto_1_009 | student009 | 5 | 76,426 |
| auto_1_010 | student010 | 5 | 49,994 |
| auto_1_011 | student011 | 5 | 48,910 |
| auto_1_012 | student012 | 5 | 70,814 |
| auto_1_013 | student013 | 5 | 47,918 |
| auto_1_014 | student014 | 5 | 70,586 |
| auto_1_015 | student015 | 5 | 53,674 |
| auto_1_016 | student016 | 5 | 50,090 |
| auto_1_017 | student017 | 5 | 42,022 |
| auto_1_018 | student018 | 5 | 41,366 |
| auto_1_019 | student019 | 5 | 39,062 |
| auto_1_020 | student020 | 5 | 65,718 |

**字段校验**：全部非空，每人 5 张图片。

### 2.2 情绪表 `push_audit_emotions`

| 情绪编码 | 记录数 | 置信度范围 | 覆盖学生数 |
|---------|--------|-----------|-----------|
| calm | 899 | 0.50 ~ 0.88 | 20 |
| sad | 296 | 0.50 ~ 0.85 | 13 |
| happy | 294 | 0.50 ~ 0.85 | 14 |
| surprised | 2 | 0.79 ~ 0.80 | 1 |

**字段校验**：
- `Id`：1,491/1,491 非空 ✅
- `student_code`：1,491/1,491 非空，且全部在学生表中存在 ✅
- `CaptureTime`：1,491 条格式全部正确（`YYYY-MM-DD HH:MM:SS`）✅
- `Emotion`：1,491/1,491 非空 ✅
- `Confidence`：1,491/1,491 非空 ✅
- `created_at`：1,491 条格式全部正确 ✅
- `GazeDirection`：全部为 `""` ✅
- `score`：全部有值 ✅
- `color`：全部有值 ✅

### 2.3 批次汇总

| 指标 | 数值 |
|------|------|
| 总批次数 | 150 |
| 每批最小记录数 | 1 |
| 每批最大记录数 | 10 |
| 平均每批记录数 | 9.9 |
| 总计 | 1,491 |

---

## 三、推送命令

### 3.1 审核模式（写入本地审核表，不调用外部 API）

```bash
cd /media/zebra/data/官渡一中初一班-0526/scripts/push

# 全量审核（推荐首次使用）
python3 push_to_external.py --audit

# 仅审核学生
python3 push_to_external.py --audit --students-only

# 仅审核情绪
python3 push_to_external.py --audit --emotions-only

# 审核前 N 个学生（快速验证）
python3 push_to_external.py --audit --top 3

# 断点续传审核
python3 push_to_external.py --audit --resume
```

### 3.2 正式推送（调用外部 API）

```bash
cd /media/zebra/data/官渡一中初一班-0526/scripts/push

# 全量推送（学生 + 情绪）
python3 push_to_external.py

# 仅推送学生
python3 push_to_external.py --students-only

# 仅推送情绪
python3 push_to_external.py --emotions-only

# 推送前 N 个学生快速测试
python3 push_to_external.py --top 3

# 自定义批次大小（默认 10，远程服务器性能弱请勿设太高）
python3 push_to_external.py --batch 20

# 断点续传（跳过已推送的记录）
python3 push_to_external.py --resume

# 干跑（不实际发送，预览请求日志）
python3 push_to_external.py --dry-run

# 从指定 emotion_record ID 开始
python3 push_to_external.py --start-id 50000
```

### 3.3 查看审核结果

```bash
# 学生审核表
PGPASSWORD=emotion psql -h localhost -U emotion -d emotion_platform -c "SELECT student_no, student_name, image_count, payload_size_bytes FROM push_audit_students ORDER BY student_id;"

# 情绪审核表（按情绪分布）
PGPASSWORD=emotion psql -h localhost -U emotion -d emotion_platform -c "SELECT emotion, count(*) as cnt, min(confidence), max(confidence), count(DISTINCT student_no) as students FROM push_audit_emotions GROUP BY emotion ORDER BY cnt DESC;"

# 批次汇总
PGPASSWORD=emotion psql -h localhost -U emotion -d emotion_platform -c "SELECT count(*) as batches, sum(record_count) as total_records, round(avg(payload_size_bytes)::numeric, 0) as avg_bytes FROM push_audit_batches;"
```

### 3.4 环境变量覆盖

```bash
# 自定义数据库
DB_HOST=192.168.1.100 DB_PORT=5432 DB_NAME=emotion_platform DB_USER=emotion DB_PASSWORD=my_password python3 push_to_external.py

# 自定义外部 API
EXTERNAL_API_BASE=http://ylcs.htface.cn:33895/api/Page/Execute CAMERA_CODE=CAM_CLASSROOM_01 python3 push_to_external.py

# 自定义项目根目录（图片文件路径）
PROJECT_ROOT=/data/project/emotion python3 push_to_external.py
```

---

## 四、注意事项

1. **审核模式** `--audit` 只写入本地审核表，不调用外部 API，用于数据验证
2. **正式推送** 不添加 `--audit` 参数，直接调用 `ylcs.htface.cn:33895` 的外部 API
3. **断点续传** `--resume` 跳过 checkpoint 中已推送的记录，适用于推送中断后继续
4. **批次大小** `--batch` 默认 10，远程服务器性能弱请勿设太高
5. **图片** 以 base64 内嵌在 JSON payload 中发送，非 HTTP URL
6. **情绪编码** 包含：happy, sad, angry, anxious, calm, surprised, fearful
